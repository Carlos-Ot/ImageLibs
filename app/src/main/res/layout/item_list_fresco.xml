<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:fresco="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="10dp"
                android:layout_marginEnd="15dp"
                android:layout_marginStart="15dp"
    >

    <com.facebook.drawee.view.SimpleDraweeView
        android:id="@+id/fresco_item_img"
        android:layout_width="@dimen/image_item_width"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        fresco:viewAspectRatio="1.77"
        fresco:placeholderImage="@drawable/placeholder_img"
        />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:contentDescription="@string/background_description"
        android:src="@drawable/text_item_background"
        />

    <TextView
        android:id="@+id/glide_item_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="@dimen/text_item_bottom"
        android:layout_marginEnd="@dimen/text_item_horizontal"
        android:layout_marginStart="@dimen/text_item_horizontal"
        android:text="@string/fresco"
        android:textColor="@color/white"
        android:textSize="@dimen/text_item_size"
        />

</RelativeLayout>